name: Deploy to Production

on:
  push:
    branches:
      - production

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up SSH key
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Connect to VM and perform actions
        run: |
          ssh denzelnasol@35.197.116.127 "\
            export PATH=$PATH:/home/denzelnasol/.nvm/versions/node/v20.0.0/bin; \
            cd investing-simulator; \
            git pull origin production; \
            echo '${{ secrets.PROD_NODE_ENV }}' > .env; \
            cd prisma; \
            echo '${{ secrets.PROD_PRISMA_ENV }}' > .env; \
            # cd ..; \
            sudo cp -R build/* /var/www/html; \
            pm2 restart app \
          "
