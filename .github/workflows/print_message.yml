name: Print Message

on:
  push:
    branches:
      - production

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Print a message
        run: echo "Hello, GitHub Actions!"
        